"use client";
import { useState } from "react";

export function UnderstandingCheck({ question, isLoading, error, onSubmit }: { question: string; isLoading: boolean; error: string | null; onSubmit: (answer: string) => Promise<void>; }) { const [answer, setAnswer] = useState(""); async function submit(value = answer) { if (!value.trim() || isLoading) return; await onSubmit(value.trim()); setAnswer(""); } return <section className="mt-8 rounded-2xl border border-indigo-100 bg-indigo-50/60 p-5" aria-labelledby="understanding-title"><p className="text-sm font-semibold uppercase tracking-wider text-indigo-700">Check your understanding</p><h2 id="understanding-title" className="mt-2 text-lg font-semibold text-slate-950">{question}</h2><textarea value={answer} onChange={(event) => setAnswer(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); void submit(); } }} maxLength={1000} rows={3} placeholder="Write your answer in your own words..." className="mt-4 w-full rounded-xl border border-indigo-200 bg-white px-3 py-3 text-sm text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500" /><div className="mt-3 flex flex-wrap gap-3"><button type="button" disabled={!answer.trim() || isLoading} onClick={() => void submit()} className="rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{isLoading ? "Checking..." : "Check my answer"}</button><button type="button" disabled={isLoading} onClick={() => void submit("I don't know yet.")} className="rounded-lg border border-indigo-200 bg-white px-4 py-2.5 text-sm font-semibold text-indigo-800">I&apos;m not sure</button></div>{error ? <p role="alert" className="mt-3 text-sm text-rose-700">{error}</p> : null}</section>; }
