import type { UnderstandingEvaluation } from "@/lib/ai/types";
const labels = { correct: "Strong understanding", partial: "Almost there", misconception: "Let’s clarify one part", uncertain: "A useful pause" };
export function UnderstandingFeedback({ evaluation, source, onAction }: { evaluation: UnderstandingEvaluation; source: "provider" | "demo"; onAction: (action: UnderstandingEvaluation["nextStep"]) => void; }) { const actionLabel = { continue: "Continue", clarify: "Clarify this part", simplify: "Make it simpler", example: "Show me an example", retry: "Try again" }[evaluation.nextStep]; return <section className="mt-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-live="polite"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold text-slate-950">{labels[evaluation.status]}</h2><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{evaluation.score}%</span></div>{source === "demo" ? <p className="mt-2 text-xs text-slate-500">Demo evaluation uses transparent topic matching.</p> : null}<p className="mt-3 text-sm leading-6 text-slate-700">{evaluation.feedback}</p>{evaluation.needsReview.length ? <p className="mt-3 text-sm text-slate-600"><span className="font-semibold text-slate-800">Focus: </span>{evaluation.needsReview.join(", ")}</p> : null}<button type="button" onClick={() => onAction(evaluation.nextStep)} className="mt-4 rounded-lg border border-indigo-200 px-4 py-2.5 text-sm font-semibold text-indigo-800">{actionLabel}</button></section>; }
