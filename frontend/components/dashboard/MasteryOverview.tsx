import type { TopicMastery } from "@/lib/mastery";
export function MasteryOverview({ entries, mastered, developing, needsReview, averageRecentScore }: { entries: TopicMastery[]; mastered: number; developing: number; needsReview: number; averageRecentScore: number | null; }) { return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm" aria-labelledby="mastery-title"><h2 id="mastery-title" className="text-xl font-semibold text-slate-950">Your learning progress</h2>{entries.length ? <><dl className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4"><div><dt className="text-xs text-slate-500">Mastered</dt><dd className="text-2xl font-semibold">{mastered}</dd></div><div><dt className="text-xs text-slate-500">Developing</dt><dd className="text-2xl font-semibold">{developing}</dd></div><div><dt className="text-xs text-slate-500">Needs review</dt><dd className="text-2xl font-semibold">{needsReview}</dd></div><div><dt className="text-xs text-slate-500">Recent score</dt><dd className="text-2xl font-semibold">{averageRecentScore ?? "-"}{averageRecentScore !== null ? "%" : ""}</dd></div></dl><div className="mt-5 space-y-2">{entries.slice(0, 5).map((entry) => <p key={entry.topicId} className="flex justify-between gap-3 text-sm"><span className="font-medium text-slate-800">{entry.topic}</span><span className="text-slate-600">Current mastery estimate: {entry.masteryLevel.replace("-", " ")}</span></p>)}</div></> : <p className="mt-3 text-sm leading-6 text-slate-600">Complete an understanding check after a lesson to begin tracking mastery.</p>}</section>; }
